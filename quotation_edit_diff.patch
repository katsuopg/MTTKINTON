*** Begin Patch
*** Update File: src/app/[locale]/(auth)/quotation/[id]/edit/QuotationEditForm.tsx
@@
-        <div className="flex justify-end gap-3">
+      </section>
+
+      <div className="flex justify-end gap-3">
         <button
           type="button"
           className="px-4 py-2 text-sm border border-gray-300 rounded-md text-gray-700 bg-white hover:bg-gray-50"
           onClick={() => router.back()}
           disabled={isSubmitting}
         >
           {language === 'ja' ? 'キャンセル' : 'Cancel'}
         </button>
         <button
           type="submit"
@@
-        </div>
-      </form>
-    </DashboardLayout>
-  );
-}
+      </div>
+    </form>
+  </DashboardLayout>
+);
+}
*** End Patch
